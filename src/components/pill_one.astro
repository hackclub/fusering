---
import guideIcon from "../assets/guide_icon.png";
import clubsIcon from "../assets/clubs_icon.png";
import submitIcon from "../assets/submit_icon.png";
import arrowIcon from "../assets/arrow_icon.png";

const { type, label, path, fill } = Astro.props;

const icons = {
  guide: guideIcon,
  clubs: clubsIcon,
  submit: submitIcon,
  arrow: arrowIcon,
};

const icon = icons[type as keyof typeof icons];

//text 3xl
---

<a
  href={path}
  class:list={[
    "flex flex-row font-baloo-2 p-4 rounded-full items-center gap-8 w-fit",
    fill === "yes"
      ? "bg-fusering-blue border-4 border-black/35 hover:shadow-card"
      : "border-4 border-text-black hover:bg-fusering-blue hover:shadow-card",
  ]}
>
  <p class="text-3xl font-semibold">{label}</p>
  {icon && <img src={icon.src} />}
</a>
